import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFF002A8F);
  static const Color secondary = Color(0xFFED1C24);
  static const Color accent = Color(0xFFFFD700);
  static const Color background = Color(0xFFF5F5F5);
  
  // Additional colors
  static const Color white = Colors.white;
  static const Color black = Colors.black;
  static const Color grey = Colors.grey;
  
  // Status colors
  static const Color confirmed = Colors.green;
  static const Color pending = Colors.amber;
  static const Color cancelled = Colors.red;
}